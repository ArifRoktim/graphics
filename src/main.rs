use std::error::Error;

//mod screen;
//mod matrix;

//use screen::Screen;
//use screen::Color;
//use matrix::Matrix;

const COLUMNS: usize = 500;
const ROWS: usize = 500;

fn main() -> Result<(), Box<dyn Error>> {

    Ok(())
}
